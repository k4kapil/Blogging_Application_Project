# database connection
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url = jdbc:mysql://localhost:3306/blogging_application
spring.datasource.username = root
spring.datasource.password = kap123

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL57Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql=true

# configuration related to file upload
spring.servlet.multipart.max-file-size= 10MB
spring.servlet.multipart.max-request-size= 10MB
# storing image to images folder
project.image= images/

logging.level.org.springframework.security=debug



#spring.security.user.name=kapil
#spring.security.user.password=kap123
#spring.security.user.roles=ADMIN